[project]
name = "gamedev-harness"
version = "0.1.0"
description = "Idea to playable: design docs, asset briefs, reference images, built assets"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "httpx>=0.27",
    "python-slugify>=8.0",
]

[tool.uv]
package = false
